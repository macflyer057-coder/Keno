name: Keno Tirage 21h00

on:
  schedule:
    - cron: '45 18 * * *'  # 20h45 CEST → arrive ~21h00 réel
  workflow_dispatch:

jobs:
  check-tirage:
    runs-on: ubuntu-latest
    steps:
      - name: Keno FDJ 21h00
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: |
          # Récupération via proxy
          RESULT=$(curl -fsS "https://api.codetabs.com/v1/proxy?quest=https://www.fdj.fr/jeux-de-tirage/keno/resultats")

          # Extraction robuste : cherche tous les nombres 1-70 dans un bloc numbers
          NUMBERS=$(echo "$RESULT" \
            | grep -oE '"numbers"[^]]+]' \
            | grep -oE '\b([1-9]|[1-6][0-9]|70)\b' \
            | head -20 \
            | tr '\n' ',')

          # Si vide, tentative fallback sur kenometric
          if [ -z "$NUMBERS" ]; then
            RESULT2=$(curl -fsS "https://kenometric.fr/tirage/$(date '+%Y-%m-%d')/")
            NUMBERS=$(echo "$RESULT2" \
              | grep -oE 'ball[^>]*>([0-9]+)<' \
              | grep -oE '[0-9]+' \
              | head -20 \
              | tr '\n' ',')
          fi

          GRILLE="1,7,8,10,12,26,30,39,41,53"
          COUNT=0
          MATCHED=""

          for N in $(echo $GRILLE | tr ',' ' '); do
            if echo ",$NUMBERS," | grep -q ",$N,"; then
              COUNT=$((COUNT+1))
              MATCHED="$MATCHED $N"
            fi
          done

          DATE=$(date '+%d/%m/%Y')

case $COUNT in
  10) GAIN_VAL=200000 ; GAIN_LIB="200000€" ;;
  9)  GAIN_VAL=2000   ; GAIN_LIB="2000€" ;;
  8)  GAIN_VAL=150    ; GAIN_LIB="150€" ;;
  7)  GAIN_VAL=15     ; GAIN_LIB="15€" ;;
  6)  GAIN_VAL=5      ; GAIN_LIB="5€" ;;
  5)  GAIN_VAL=2      ; GAIN_LIB="2€" ;;
  0)  GAIN_VAL=2      ; GAIN_LIB="2€" ;;
  *)  GAIN_VAL=0      ; GAIN_LIB="0€" ;;
esac

          if [ $COUNT -ge 6 ]; then
            TITRE="🎉 $COUNT/10 ($GAIN) !"
            MSG="Numéros :$MATCHED — $DATE"
            PRIORITY="urgent"
            TAGS="tada"
          else
            TITRE="🎱 $DATE — $COUNT/10 ($GAIN)"
            MSG="Tirage récupéré : $NUMBERS"
            PRIORITY="default"
            TAGS="dart"
          fi

          echo "[$TITRE] $MSG"
          echo "NUMBERS=$NUMBERS"
          echo "COUNT=$COUNT"

          curl -fsS -X POST "https://ntfy.sh/$NTFY_TOPIC" \
            -H "Title: $TITRE" \
            -H "Priority: $PRIORITY" \
            -H "Tags: $TAGS" \
            -d "$MSG"
